# v2 EEF Follow Context Snapshot

- Task statement: add v1-style EEF following to the v2 FoundationPose visual servoing flow.
- Desired outcome: after v2 estimates object pose, `ee_right` should follow the object with robot Cartesian position commands.
- Stated solution: push current committed work first, then clarify implementation with `$deep-interview`.
- Probable intent hypothesis: use v2/remote FoundationPose pose estimation as the perception source while restoring the practical v1 behavior where the robot hand follows the tracked object.
- Known facts/evidence:
  - `git push kaiseong main` returned `Everything up-to-date`; HEAD is `30dadb0 Move hybrid register work onto the v2 server`.
  - `visual_servoing.visual_servo_client_v2` is the Track Remote command path from the GUI.
  - `visual_servoing.scripts.fp_track_live` is used for Track Local and Track Hybrid; Hybrid uses server-side register and local tracking.
  - v1 robot control exists in `visual_servoing.visual_servo_client` / `_v1` via `RobotContext` and `send_right_arm_cartesian()` with `rby.CartesianCommandBuilder`.
  - Current worktree contains pre-existing dirty files plus an interrupted local EEF-follow draft; it is not execution-ready.
- Constraints:
  - Preserve v1 code unless explicitly requested otherwise.
  - Avoid committing/pushing broken partial work.
  - Start with requirements clarification before implementation because the target v2 execution path is a product decision.
- Unknowns/open questions:
  - Which v2 tracking path should be considered the primary success path: Track Remote, Track Hybrid, Track Local, or all?
  - Should the first implementation be position-only with current EE rotation preserved, or should it use FoundationPose object orientation for wrist orientation?
  - Should offsets be specified in T5 frame, object frame, or both?
  - What GUI controls are required for safe robot execution?
- Decision-boundary unknowns:
  - Whether OMX may choose the first supported mode and leave the rest as out of scope.
  - Whether execute mode should be CLI-only at first or exposed in GUI immediately.
  - Whether dry-run output is enough before real robot validation.
- Likely codebase touchpoints:
  - `visual_servoing/visual_servo_client_v2.py`
  - `visual_servoing/foundationpose_model_free/gui_app.py`
  - `visual_servoing/scripts/fp_track_live.py`
  - `visual_servoing/visual_servo_client.py`
  - `visual_servoing/tests/test_visual_servo_client_v2.py`
  - `visual_servoing/tests/foundationpose_model_free/test_gui_app.py`
- Prompt-safe initial-context summary status: not_needed
