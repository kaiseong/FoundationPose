# Deep Interview: v2 EEF Follow

- Interview ID: `v2-eef-follow-20260608T070515Z`
- Profile: standard
- Context type: brownfield
- Final ambiguity: 0.15
- Threshold: 0.20
- Context snapshot: `.omx/context/v2-eef-follow-20260608T070515Z.md`

## Summary

The first v2 EEF-follow implementation should target the GUI `Track Hybrid` flow. It must use the server-side FoundationPose register path plus local tracking, then send real robot Cartesian commands so `ee_right` follows the tracked object pose.

## Transcript

1. Scope
   - Question: Which v2 execution path is the first success target?
   - Answer: `Track Hybrid`.
   - Effect: `fp_track_live.py` / GUI Track Hybrid is the primary implementation path.

2. Orientation
   - Question: Position-only or wrist follows FoundationPose orientation?
   - Answer: Wrist should follow.
   - Effect: Use full pose, not only translation.

3. Object-to-EE Rule
   - Question: How should `object_T_ee` be defined?
   - Answer: The right hand pose should match the object pose with the object x-axis rotated 180 degrees, and be offset -0.2 m along the object x-axis.
   - Effect: Use a fixed object-frame command transform: `target_t5_T_ee = t5_T_object @ object_T_ee`, with `object_T_ee` containing x translation `-0.2` and x-axis rotation `pi`, subject to matrix-order tests.

4. Success Criteria
   - Question: Dry-run only or real robot execution?
   - Answer: Real robot execution is required. The fixed rule is based on the right-hand coordinate convention verified with `visual_servoing/cartesian_keyboard_test.py`.
   - Effect: Acceptance includes `--execute` real robot following.

5. Safety
   - Question: What step limits should be used?
   - Answer: Limit each EE command translation step to 3 cm.
   - Effect: Default `max_translation_step_m` should be `0.03`.

6. Non-goals
   - Question: Confirm first-pass exclusions.
   - Answer: Confirmed.
   - Effect: Do not modify v1; do not add EEF follow to Track Remote/Local; do not add automatic offset capture; avoid GUI redesign; skip unsafe pose jumps.

## Brownfield Evidence

- `visual_servoing/foundationpose_model_free/gui_app.py` uses `visual_servoing.scripts.fp_track_live` for `Track Hybrid`.
- `visual_servoing/scripts/fp_track_live.py` supports `--remote-register-server`, which is the current Hybrid register path.
- `visual_servoing/visual_servo_client.py` and `visual_servoing/visual_servo_client_v1.py` contain `RobotContext` and `send_right_arm_cartesian()` using `rby.CartesianCommandBuilder`.
- `visual_servoing/cartesian_keyboard_test.py` verifies the right-hand T5 Cartesian convention, including `robot-plus-x-left-y-up-z`.

## Readiness

- Non-goals: explicit.
- Decision boundaries: explicit.
- Pressure pass: complete; the proposed automatic relative-pose capture was rejected and replaced with a fixed object-frame rule.
- Ready for planning/execution handoff.
